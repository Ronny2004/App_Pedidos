<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permisos -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Actividad de Splash (Pantalla de Bienvenida) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad de Login (Inicio de sesión) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true" />

        <!-- Actividad principal (MainActivity) -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- Actividad de Administrador -->
        <activity
            android:name=".AdminActivity"
            android:exported="true" />

        <!-- Actividad de Mesero -->
        <activity
            android:name=".WaiterActivity"
            android:exported="true" />

        <!-- Actividad de Cocinero -->
        <activity
            android:name=".CookActivity"
            android:exported="true" />

        <!-- Actividad de Cajero -->
        <activity
            android:name=".CashierActivity"
            android:exported="true" />

        <!-- Actividad de Gestión de Productos -->
        <activity
            android:name=".ProductoFormActivity"
            android:exported="true" />

    </application>

</manifest>